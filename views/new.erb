<div>
    <% unless current_user.nil? %>
        <form action="/new" method="POST" enctype="multipart/form-data">
            <div class="form">
                <label for="name">名前</label><br>
                <input name="name" id="name" required class="form-control" value="<%= current_user.familyname %> <%= current_user.firstname %>"><br>
                <label for="image">画像を選択</label><br>
                <input type="file" id:image name="file" id="image"></p>
            </div>
            <div>
                <input type="submit" class="btn btn-success" value="投稿">
            </div>
        </form>
    <% else %>
        <h3>まず、<a href="/signin">ログイン</a>してください、</h3>
    <% end %>
</div>
